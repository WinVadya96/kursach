@using kursach.Models
@model kursach.Models.ApplicationUser

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>UserCollection</h4>
        <hr />
        <fieldset>
            <legend>User</legend>
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.UserName)

            <div class="editor-label"><b>Имя</b></div>
            <div class="editor-field">
                @Html.EditorFor(model => model.UserName)
            </div>
            @* TO DO: раскоментировать и доделать!!! *@

            @*<div class="editor-label"><b>Collections</b></div>
            @{
                List<Collection> collections = ViewBag.Collections;
                foreach (Collection c in collections)
                {
                    <input type="checkbox" name="selectedCollections" value="@c.Id"
                    @(ViewBag.Collections.Contains(c) ? "checked=\"checked\"" : "") />@c.Name <br />
                }
            }*@

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Save" class="btn btn-default" />
                </div>
            </div>
        </fieldset>
    </div>
}
